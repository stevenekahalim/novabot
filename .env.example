# ===================================
# APEX ASSISTANT V3 - Environment Variables
# Architecture: Pure Conversational (V3 ONLY)
# ===================================

# WhatsApp Configuration
WHATSAPP_SESSION_PATH=./whatsapp-session

# OpenAI Configuration (V3)
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL_RESPONSE=gpt-4-turbo  # Used by V3 ResponseGenerator (auto-selects based on context)
OPENAI_SPENDING_LIMIT=100

# Supabase Configuration (V3 Tables: messages_v3, hourly_notes)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-key  # REQUIRED: Must use service_role key, not anon key

# Application Settings
TIMEZONE=Asia/Jakarta  # Used for daily digest scheduling
LOG_LEVEL=info
PORT=3000

# WhatsApp Group (will be auto-detected by V3, but can override)
TARGET_GROUP_NAME=APEX Projects

# ===================================
# REMOVED VARIABLES (V2 only, no longer used):
# - USE_V3 (always V3 now)
# - NOTION_TOKEN (Notion not used in V3)
# - SUPABASE_KEY (anon key - V3 uses service key only)
# - OPENAI_MODEL_CLASSIFICATION (V2 classifier removed)
# - DAILY_DIGEST_TIME (V3 uses midnight Jakarta time)
# - FOLLOWUP_DAYS (V2 feature, removed)
# ===================================
